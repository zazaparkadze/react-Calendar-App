const handleNewEmployee = (
    newEmployee,
    allEmployees,
    setAllEmployees,
    schedule,
    setSchedule
) => {
    console.log('new employee/ new user created');
    console.log(newEmployee);
    // update allEmployees list, first state then db

    // make new name Entry

    // update schedule, first state, then db
};

export default handleNewEmployee;
